@model CreateRoleViewModel

<form asp-controller="Roles" asp-action="Create" class="form-inline my-2">
    <input asp-for="Name" class="form-control" placeholder="New Role name"/>
    <button type="submit" class="btn btn-primary ml-2">
        Add
    </button>
</form>